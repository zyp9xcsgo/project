const math = {
    perimeter : function (x, y) {
        
        return (x + y) * 2;
    },
    square : function (x, y) {
        return x * y;
    },
}

console.log(perimeter(10, 20), square(10, 20));