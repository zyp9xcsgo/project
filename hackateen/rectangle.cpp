#include<iostream>
#include<vector>

using namespace std;

int main() {
    int n, a, multi = 1;
    cin >> n;
    vector<int> num;
    for (int i = 0; i < n; i++) {
        cin >> a;
        num.push_back(a);
    }
    
}